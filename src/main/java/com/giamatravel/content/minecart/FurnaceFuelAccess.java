package com.giamatravel.content.minecart;

/** Implemented (via mixin) by the furnace minecart so other code can see if it is fuelled. */
public interface FurnaceFuelAccess {
	boolean giamatravel$hasFuel();
}
